@model IEnumerable<Shop.Common.ViewModels.SettingViewModel>

@{
    ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DefaultCurrencyId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DefaultCurrencyFullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DefaultPriceLevelId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DefaultPriceLevelName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultCurrencyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultCurrencyFullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultPriceLevelId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultPriceLevelName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
